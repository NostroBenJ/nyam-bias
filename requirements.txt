fastapi==0.115.0
uvicorn==0.30.6
apscheduler==3.10.4
# --- only needed when you flip to live data (NYAM_MOCK=0) ---
yfinance==0.2.44
# --- only needed if you wire in the Claude brief (set ANTHROPIC_API_KEY) ---
anthropic==0.39.0
